{
  "name": "kaneo-mcp",
  "version": "0.1.0",
  "description": "MCP server for Kaneo project management — lets AI assistants read and manage projects, tasks, columns, comments, and labels.",
  "type": "module",
  "bin": {
    "kaneo-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  }
}
